Tools 4.0 - Jul 15, 2014 14:27

- a lot of renames including
    udef -> udf
    type -> typ
    gcls -> cls
    nullp -> nulp
    push -> psh
    disp -> dsp
    slc -> sli
    add -> app
    group -> grp
    split -> spl
    apd -> att
    tail -> tai
    alref -> aref
    strf -> stf
    apply -> apl
    head -> sig
    mtch -> mat
    speed -> spd
- support for more arraylike types
- support for html elements
- added fold
- new hash table system (oref, oset, odel, ...)
